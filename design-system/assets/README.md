# Assets

- `logo-orb.html` — the canonical AGENT-33 mark. CSS-only conic-gradient orb with the `pulse-orb` 4s animation. Use the markup/keyframes from `colors_and_type.css` to inline into any product surface.
- `wordmark.svg` — wordmark mock pairing the orb with **AGENT-33** in Space Grotesk. Mock only — replace if a real logotype is commissioned.

There are no other brand assets (illustrations, photography, patterns) in the source repo. The product is panels and data — not imagery.
